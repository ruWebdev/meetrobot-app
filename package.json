{
    "name": "meetrobot-monorepo",
    "version": "0.0.1",
    "private": true,
    "description": "Telegram-first workspace-based events and bookings platform",
    "workspaces": [
        "backend",
        "frontend"
    ],
    "scripts": {
        "lint": "echo \"Add root linting if needed\"",
        "format": "echo \"Add root formatting if needed\"",
        "backend:dev": "npm run start:dev --workspace backend",
        "backend:build": "npm run build --workspace backend",
        "frontend:dev": "npm run dev --workspace frontend",
        "frontend:build": "npm run build --workspace frontend"
    }
}